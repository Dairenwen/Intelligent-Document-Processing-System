问题
1. 智能填表结果为空（.xlsx ->.xlsx）；
2. 填表时 切换页面，会回到填表前
3. .xlsx -> .docx('{{专业}}{{性别}}' -> '大数据男')
4. 我放在测试集的 测试0305 里了

疑似问题
1. 登录的用户能看到其他用户上传的文件
2. 该系统的三个功能，在首页出现了三个个入口（左侧列表，右侧则是最明显三个按钮）
3. 首页的“三大核心模块”分布指向三个功能（同上一点）

前端准备修改（额度用完了
1. 模板上传后的页面布局
2. 填表完成后的页面布局

最近的修改
1. 填充时重复的进度信息
2. 填充时百分比数字格式
3. 填充时的面板
4. 移除 首页的“快速操作”
5. 注释了 侧边栏的功能说明

可拓展
1. 图表生成
2. 在线创建并编辑模板