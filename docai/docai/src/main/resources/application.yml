server:
  port: 8080
  tomcat:
    max-threads: 200
    connection-timeout: 120000

spring:
  datasource:
    url: jdbc:mysql://124.222.53.34:3306/docai?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: drw
    password: dairenwen1092
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 200MB
  ai:
    dashscope:
      api-key: sk-c02ef573727b4c049549c01bf7d523b2
      chat:
        options:
          model: qwen-plus #可以选择qwen-max
          temperature: 0.3 #输出随机性

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 文件上传路径
file:
  upload-dir: ./uploads/


# 日志级别
logging:
  level:
    com.team.docai: DEBUG
    root: INFO
